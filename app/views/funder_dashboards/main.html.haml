.web_body
  .container{style: "width: 1200px;"}
    = render 'funder_sidebar'
    - @leads.each do |lead|
      .funder-sidebar.col-sm-3
      = render 'funder_lead', lead: lead, is_make_offer: true
      
    = will_paginate @leads
      